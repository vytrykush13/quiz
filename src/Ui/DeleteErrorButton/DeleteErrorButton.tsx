import React from 'react';
import css from './DeleteErrorButton.module.scss'

const DeleteErrorButton = () => {
    return (
        <div className={css.wrapper}>
            
        </div>
    );
};

export default DeleteErrorButton;